const INITIAL_STATE = {
  logged: false,
  error: false
}
const loginReducer = (state=INITIAL_STATE, action) => {
  switch(action.type) {
    default: 
      return state
  }
}

export default loginReducer;